# Handoff: S03-T03

- task_id: S03-T03
- agent: A2(핵심 업무 백엔드)
- status: PASS
- summary: 세션 검증(SessionGuard/SessionVerifier/FakeSessionVerifier), 운영자 세부 권한 검사(OperatorPermissionGuard), 공통 오류 모델(DomainError/AllExceptionsFilter), 요청 ID 추적(RequestIdMiddleware), 상태 확인 API(/health, /health/whoami)를 구현했다. CORS는 credentials 포함으로 apps/web 오리진만 허용하도록 구성했다. 운영 환경(APP_ENV=production)에서 AUTH_TEST_BYPASS=true 조합을 부팅 단계와 SessionGuard 생성자 양쪽에서 이중 차단하도록 만들었고, 실제로 그 조합으로 부팅을 시도해 프로세스가 즉시 종료됨을 확인했다. VerificationsController로 "본인/심사 담당 운영자만 비공개 증빙 조회 가능" 규칙의 최소 실사례를 구현하고 제3자 조회가 403으로 거절됨을 실제 HTTP 요청과 자동 테스트 양쪽으로 검증했다.
- changed_files:
  - apps/api/src/main.ts (CORS 추가)
  - apps/api/src/common/request-id.middleware.ts, errors.ts, http-exception.filter.ts
  - apps/api/src/modules/auth/session-user.ts, session-verifier.ts, session.guard.ts, current-user.decorator.ts, operator-permission.guard.ts
  - apps/api/src/modules/verifications/verifications.controller.ts, verifications.module.ts, verifications.repository.ts
  - apps/api/src/modules/health/health.controller.ts
  - apps/api/src/app.module.ts
  - docs/backend/auth-foundation.md
- evidence:
  - base_revision: docs/api/authorization.md(S02-T01), docs/decisions/policies.md(인증 계약 미체결 사실)
  - checks:
    - "GET /health/whoami 쿠키 없음 → 401 (실제 curl 요청 및 자동 테스트로 검증)"
    - "GET /health/whoami 존재하지 않는 세션 키 → 401"
    - "GET /health/whoami fake-learner-prep(affiliationType=prep, campusId=null) → 200, SRC-01 준수 확인"
    - "GET /health 인증 없이 → 200"
    - "본인 증빙 GET /verifications/:id → 200, 제3자 → 403 FORBIDDEN, 심사 담당 운영자 → 200"
    - "APP_ENV=production AUTH_TEST_BYPASS=true → 부팅 실패(exit code 1, listen 로그 없음) 실제 프로세스 실행으로 확인"
    - "APP_ENV=production(우회 없음) → 정상 부팅하되 whoami는 항상 401(실제 Supabase 미연동을 숨기지 않음)"
  - not_run:
    - "실제 Supabase Auth 연동 테스트 — 계약 미체결로 대상 자체가 없음"
  - policy_and_metric_versions: "해당 없음(인증/인가 계층, 정책 버전 참조 없음)"
- remaining_work:
  - "FakeSessionVerifier 픽스처를 실제 DB users 레코드와 연결(S04)"
  - "Supabase Auth 계약 확정 시 SessionVerifier 구현체 교체"
  - "RLS 또는 동등한 DB 레벨 접근 제어 도입 여부 결정"
- contract_requests: []
- next_owner: A3 (S03-T04)
- source_requirement_evidence:
  - "SRC-02: 항목별 독립 확인 상태를 SessionUser.identityVerificationStatus와 verifications 접근 제어로 구현·검증"
