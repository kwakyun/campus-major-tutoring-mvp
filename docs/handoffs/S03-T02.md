# Handoff: S03-T02

- task_id: S03-T02
- agent: A1(아키텍처·DB·API 계약)
- status: PASS
- summary: data-model.md·state-machines.md(S02-T01)를 반영한 초기 마이그레이션 2개 파일(확장/정책/학교·생활권/사용자·인증, 수업/협의/예약/납부/결과/정산)과 허구 데모 시드를 작성했다. 실제 PostgreSQL 16 인스턴스에 빈 DB부터 적용해 성공을 확인했고, 재실행 시 멱등(스킵)함을 확인했다. 특히 S02-T06 QA 검토에서 OPEN으로 남았던 F-02(자기 거래 DB 제약 미설계)를 `conversations.no_self_dealing` CHECK 제약으로 실제 구현하고, 동일인 삽입 실패·타인 삽입 성공을 실제 DB에 대해 재현해 RESOLVED로 전환했다.
- changed_files:
  - db/migrations/0001_extensions_and_identity.sql
  - db/migrations/0002_catalog_and_transactions.sql
  - db/seeds/demo.sql
  - docs/architecture/migration-report.md
- evidence:
  - base_revision: docs/architecture/data-model.md, state-machines.md(S02-T01), docs/qa/S02-design-review.md(F-02)
  - checks:
    - "pnpm run db:migrate(빈 DB) 실제 실행 — 2개 파일 신규 적용 성공"
    - "pnpm run db:migrate 재실행 — schema_migrations 이력 확인 후 2건 모두 skip(멱등성 확인)"
    - "pnpm run db:seed 실제 실행 — 성공"
    - "F-02 재현 테스트: 동일 tutor_id로 tutor/learner를 모두 지정한 INSERT → 실제 CHECK 제약 위반 에러 발생 확인. 서로 다른 사용자 INSERT → 성공 확인"
  - not_run:
    - "관리형 클라우드 DB(Supabase 등)에서의 동일 마이그레이션 적용 — 서비스 계약 미체결로 대상 환경 자체가 없음"
  - policy_and_metric_versions: "policy_versions 시드에 '2026-09-fee-15pct'(1500bps) 1건 포함 — docs/decisions/policies.md 확정 수치와 일치"
- remaining_work:
  - "waitlist_entries/learning_outcomes의 본인+운영자 전용 접근을 애플리케이션 계층에서 구현(S04, A2)"
  - "curriculum_versions 소유자 전용 복제 검사를 서비스 계층에서 구현(S04, A2)"
  - "FakeSessionVerifier 픽스처와 db/seeds/demo.sql의 사용자 레코드를 실제로 연결(S04, A2) — 현재는 두 세계가 분리되어 있음을 db/seeds/demo.sql 주석에 명시"
- contract_requests: []
- next_owner: A2 (S03-T03)
- source_requirement_evidence:
  - "SRC-01: user_campus_affiliations.affiliation_type='prep'|'none'이고 campus_id가 NULL이어도 저장 가능함을 시드로 실증(campus_required_when_enrolled 제약은 'enrolled'일 때만 campus_id 필수)"
  - "SRC-02: verifications.status가 항목(type)별 독립 상태를 가짐을 스키마와 시드(school_enrollment=verified, certification=pending 동시 존재)로 실증"
  - "SRC-08: campuses/life_zones/campus_life_zones 분리를 스키마로 구현"
