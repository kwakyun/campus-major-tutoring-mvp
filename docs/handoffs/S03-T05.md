# Handoff: S03-T05

- task_id: S03-T05
- agent: A6(QA·품질 검증)
- status: PASS
- summary: 새 개발 환경 기준(클라우드 리눅스 샌드박스, PostgreSQL 16 로컬 클러스터)으로 설치·타입체크·빌드·단위/통합테스트·DB 마이그레이션·시드를 실제로 실행해 전부 통과를 확인했다. 인증 성공/실패, 편입준비생(SRC-01) 정상 진입, 타인 증빙 조회 거절(403), 심사 담당 운영자의 타인 증빙 조회 허용, 운영 환경 테스트 우회 이중 차단을 실제 HTTP 요청과 자동화된 통합 테스트(7건)로 검증했다. 세션 자연 만료(TTL) 시나리오는 FakeSessionVerifier에 만료 개념이 없어 NOT_APPLICABLE로 명확히 구분했다. 미실행 항목(브라우저 육안 확인, GitHub Actions 실제 실행, Windows 로컬 재현)은 숨기지 않고 그대로 보고했다.
- changed_files:
  - apps/api/test/auth-and-verifications.e2e-spec.ts
  - apps/api/vitest.config.ts
  - apps/api/package.json (test 스크립트를 `pnpm run build && vitest run`으로 변경 — 사유는 docs/qa/S03-foundation.md §3-1)
  - tests/integration/README.md
  - tests/e2e/README.md
  - docs/qa/S03-foundation.md
- evidence:
  - base_revision: S03-T01~T04 산출물 전체(2026-09-19)
  - checks:
    - "pnpm install/typecheck/build/test 전체 실제 실행 — PASS(docs/qa/S03-foundation.md §1)"
    - "pnpm run db:migrate(빈 DB)/재실행/db:seed 실제 실행 — PASS, 멱등성 확인(§2)"
    - "F-02 자기거래 CHECK 제약 실제 재현(§2-1)"
    - "자동화 통합테스트 7건 전부 통과(§3-1)"
    - "수동 curl 시나리오 10건 전부 예상대로 동작(§3)"
  - not_run:
    - "apps/web 실제 브라우저 렌더링 확인 — GUI 없음"
    - "production 부팅 차단의 자동 회귀 테스트화 — 별도 프로세스 스폰 필요, 수동만 검증"
    - "GitHub Actions 실제 실행 — 리포지토리 미푸시"
    - "PostgreSQL 동시성 경쟁조건 테스트 — S02-design-review.md F-05가 이미 S04/S05 대상으로 지정, 범위 밖"
    - "Windows 로컬 환경 재현 — 사용자 확인 필요"
  - policy_and_metric_versions: "packages/contracts/openapi.yaml 0.2.0-s02-baseline 참조, 변경 없음"
- remaining_work:
  - "위 not_run 항목 전부(docs/qa/S03-foundation.md §5에 표로 정리)"
- contract_requests: []
- next_owner: A0 (S03-GATE 통합 판정)
- source_requirement_evidence:
  - "SRC-01: 편입준비생 정상 진입을 자동 테스트로 고정(auth-and-verifications.e2e-spec.ts)"
  - "SRC-02: 상대방 증빙 조회 거절(403)을 자동 테스트로 고정, 교육자 확인 상태 위조 가능성은 DB verified_status가 애플리케이션(운영자 승인)만 변경 가능하도록 설계됨을 코드 검토로 확인(직접 위조 경로 없음 — 사용자 입력 필드가 아님)"

## 추가 회차 — 2026-09-19b (실제 브라우저 E2E 회차)

이전 `not_run` 항목 중 "apps/web 실제 브라우저 렌더링 확인"을 GUI 브라우저(Playwright/Chromium)가 있는
환경에서 실제로 수행했다. 이 회차에서 위 PASS 판정과 무관하게 **신규 결함 3건**을 발견했다(그중 1건은
실제 수정 후 재검증까지 완료). 원래 판정(status: PASS)은 되돌리지 않고 그대로 남기되, 아래 내용을
반드시 함께 확인해야 한다 — 자세한 재현·근거는 `docs/qa/S03-foundation-recheck-2026-09-19.md` 전체를 참고.

- **`.github/workflows/ci.yml`이 실제로 존재하지 않음**(changed_files·S03-GATE가 인용한 CI 골격과 실물 불일치) — A7 확인 필요
- **`NODE_ENV=development`가 셸에 있으면 `pnpm run build`(apps/web)가 재현 가능하게 실패**(문서 §5가 권장하는 `.env` 전체 export와 충돌) — A3/A7 확인 필요
- **`NEXT_PUBLIC_API_BASE_URL` 기본값에 잘못된 `/api` 접두사** → 실제 브라우저 로그인 흐름이 100% 실패(수정 후 5/5 Playwright 시나리오 PASS로 재검증 완료, 스크린샷 `docs/qa/evidence/S03-recheck-2026-09-19/`) — A3(+A1/A2 계약 조율) 확인 필요
- (재확인) `pnpm install --frozen-lockfile`이 apps/api의 테스트 의존성 3건 누락으로 실패 — A7 확인 필요

next_owner를 A0로 유지하되, 위 4건은 S03-GATE의 "인증·환경 분리·CI와 스테이징 준비 상태가 검증됨"
판정 재검토 대상으로 넘긴다.
