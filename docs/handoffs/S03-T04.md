# Handoff: S03-T04

- task_id: S03-T04
- agent: A3(UX·프론트엔드)
- status: PASS
- summary: 최소 공유 UI 컴포넌트(Button/FormField/StatusBadge, packages/ui)와 로그인/세션 확인 화면(apps/web/src/app/login)을 구현해 로그인·세션 조회·로그아웃 흐름을 실제로 연결했다. apps/api 호출 클라이언트(api-client.ts)는 세션 쿠키를 credentials: "include"로 전송하며, 서버 응답(401/403 등)의 code/message를 그대로 화면에 노출해 "화면 숨김을 서버 권한 검사의 대체로 쓰지 않는다"는 원칙을 지켰다 — 즉 화면은 서버가 실제로 거절한 이유를 보여줄 뿐, 스스로 권한을 판단해 버튼을 감추는 방식에 의존하지 않는다.
- changed_files:
  - packages/ui/package.json, tsconfig.json, src/index.ts, src/button.tsx, src/form-field.tsx, src/status-badge.tsx
  - apps/web/src/app/layout.tsx, page.tsx, login/page.tsx
  - apps/web/src/lib/api-client.ts
  - docs/ux/foundation-check.md
- evidence:
  - base_revision: docs/ux/screens.md(S02-T02), docs/backend/auth-foundation.md(S03-T03)
  - checks:
    - "pnpm run build(apps/web, next build) 실제 실행 — /, /login, /_not-found 3개 라우트 정적 생성 성공"
    - "pnpm run typecheck(apps/web) 실제 실행 — 통과"
  - not_run:
    - "실제 브라우저에서의 육안 렌더링 확인(pnpm run dev:web) — 이 클라우드 샌드박스에 GUI 브라우저가 없음. 사용자 로컬 환경에서 확인 권장(docs/ux/foundation-check.md §4)"
    - "모바일 화면 확인 — 위와 같은 이유로 미실행. CSS/반응형 스타일 자체가 아직 미정(디자인 확정 전, packages/ui는 최소 골격)이므로 이번 단계에서는 해당 인수 기준을 충족했다고 보고하지 않는다"
  - policy_and_metric_versions: "해당 없음"
- remaining_work:
  - "모바일 반응형 스타일 확정(디자인 시스템 도입 이후, S10 트랙 후보) — 지금은 정직하게 미완료로 보고"
  - "실제 로그인 폼(이메일/비밀번호 또는 OAuth)으로 교체 — 인증 제공자 계약 확정 후"
  - "탐색/상담/예약/완료 화면 구현 — S04 이후"
- contract_requests: []
- next_owner: A6 (S03-T05)
- source_requirement_evidence: []
