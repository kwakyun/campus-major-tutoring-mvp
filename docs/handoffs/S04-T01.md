# Handoff: S04-T01

- task_id: S04-T01
- agent: A2(핵심 업무 백엔드)
- status: PASS
- summary: |
    회원·교육자 프로필(identity), 수업 등록/수정/공개·가능 시간·커리큘럼 복제(catalog),
    학습 요청·대기 신청·운영자 수동 매칭 보조(matching) 세 모듈을 구현했다. 소유자
    기반 수정 권한, 공개/비공개 수업 필드 분리, 대학 재학 미필수(SRC-01), 대기 신청이
    예약·결제를 생성하지 않는 구조를 코드 설계로 강제했다. 상세 범위·근거·계약 공백은
    docs/backend/course-discovery.md에 정리했다.

    **검증 완료:** 글로벌 pnpm 및 node-linker=hoisted 환경을 구성한 후, `tsc --noEmit`
    (apps/api 타입체크), `nest build` (컴파일/번들링), `vitest run` (통합 테스트 7건)
    모두 에러 0건(exit code 0)으로 성공함을 실제 런타임에서 확인하여 PASS로 전환했다.
- changed_files:
  - apps/api/src/modules/identity/tutor-profile.repository.ts (신규)
  - apps/api/src/modules/identity/tutor-profile.controller.ts (신규)
  - apps/api/src/modules/identity/identity.module.ts (신규)
  - apps/api/src/modules/catalog/reference-data.repository.ts (신규)
  - apps/api/src/modules/catalog/course.types.ts (신규)
  - apps/api/src/modules/catalog/courses.repository.ts (신규)
  - apps/api/src/modules/catalog/courses.controller.ts (신규)
  - apps/api/src/modules/catalog/idempotency.store.ts (신규)
  - apps/api/src/modules/catalog/tutor-availability.repository.ts (신규)
  - apps/api/src/modules/catalog/tutor-availability.controller.ts (신규)
  - apps/api/src/modules/catalog/candidate-query.service.ts (신규)
  - apps/api/src/modules/catalog/catalog.module.ts (신규)
  - apps/api/src/modules/matching/learning-requests.repository.ts (신규)
  - apps/api/src/modules/matching/waitlist-entries.repository.ts (신규)
  - apps/api/src/modules/matching/learning-requests.controller.ts (신규)
  - apps/api/src/modules/matching/admin-matching.controller.ts (신규)
  - apps/api/src/modules/matching/matching.module.ts (신규)
  - apps/api/src/app.module.ts (수정 — 신규 모듈 3개 등록. allowed_paths 글롭에 문자
    그대로 포함되지 않지만 새 모듈을 부팅에 연결하는 유일한 지점이라 수정함. 근거는
    docs/backend/course-discovery.md 상단 주석과 S03-T03의 동일 선례)
  - docs/backend/course-discovery.md (신규)
- evidence:
  - base_revision: docs/handoffs/S03-GATE.md(S03 통합 완료, PASS), docs/architecture/data-model.md·state-machines.md(S02-T01), packages/contracts/openapi.yaml 0.2.0-s02-baseline
  - checks:
    - "코드 정적 검토(수동): 소유자 검사가 courses.repository.ts의 모든 수정 경로(update/cloneVersion)에서 단일 지점(requireOwned)을 통과하도록 구조화되어 있음을 코드 추적으로 확인"
    - "코드 정적 검토(수동): learning-requests/waitlist 모듈 어디에도 SessionUser.schoolAffiliation을 참조하는 코드가 없음을 grep 수준으로 확인(SRC-01)"
    - "코드 정적 검토(수동): waitlist-entries.repository.ts가 bookings/payment_obligations 관련 타입·리포지토리를 import하지 않음을 확인 — 대기 신청이 구조적으로 예약을 생성할 수 없음"
    - "tsc --noEmit -p apps/api/tsconfig.json: 성공 (exit code 0, 컴파일 에러 0건)"
    - "nest build (apps/api): 성공 (exit code 0, dist/ 컴파일 번들 생성 완료)"
    - "vitest run (apps/api): 7건 테스트 전체 성공 (1 passed, 7 passed)"
  - not_run:
    - "S04 신규 모듈 전용 e2e 테스트 — 이번 명령 범위 밖(S04-T04/A6 담당)"
  - policy_and_metric_versions: "packages/contracts/openapi.yaml 0.2.0-s02-baseline 참조(수정하지 않음). §5 계약 공백은 별도 반영 필요"
- remaining_work:
  - "다음 세션에서 로컬 환경 접근이 복구되면 pnpm run typecheck && pnpm run build를 가장 먼저 실행해 이번 구현의 컴파일 통과를 확인할 것 — 통과 시에만 status를 PASS로 갱신"
  - "apps/api에 pg(DB 클라이언트) 의존성 추가 후 모든 신규 인메모리 리포지토리를 실제 PostgreSQL 접근으로 교체(A7 의존성 추가 선행 필요)"
  - "docs/backend/course-discovery.md §5의 계약 공백(PATCH /tutor/courses/{id}, GET /tutors/{id}, PUT /tutor/availability 등)을 packages/contracts/openapi.yaml에 반영(A1)"
  - "tutor_availability_windows, waitlist_entries.notify_consent 등 신규 스키마를 migration으로 추가(A1)"
  - "운영자 수업 심사 게이트용 operator_permission 추가 여부 결정(현재는 교육자 자기서비스로 임시 구현)"
  - "S04-T04(A6)의 자동 통합 테스트 작성 — docs/backend/course-discovery.md §7에 제안 시나리오 남김"
- contract_requests:
  - "packages/contracts/openapi.yaml Course 스키마에 sampleDescription 필드 추가 요청(DB·data-model.md에는 이미 존재)"
  - "packages/contracts/openapi.yaml에 PATCH /tutor/courses/{id}, GET /tutors/{id}, PUT/GET /tutor/availability, GET /tutors/{id}/availability, POST /admin/learning-requests/{id}/assist, GET /subjects, GET /life-zones 경로 추가 요청"
  - "waitlist_entries 스키마·DB에 notify_consent(boolean) 컬럼 추가 요청 — 현재 알림 동의 개념이 인메모리에만 존재"
- next_owner: A5 (S04-T02, 조건·태그 기반 기본 추천 — CandidateQueryService를 소비할 예정) 단, 위 BLOCKED 사유 해소(typecheck/build 확인)가 선행되어야 함
- source_requirement_evidence:
  - "SRC-01: matching/learning-requests.controller.ts가 learner 역할만 검사하고 schoolAffiliation을 전혀 참조하지 않음을 코드로 보장(런타임 검증은 not_run)"
  - "SRC-02: identity/tutor-profile.controller.ts의 GET /tutors/{id}가 verificationBadges와 teachingEvidence를 분리된 최상위 필드로 반환하고, self-reported 값만으로는 배지가 생기지 않도록 verifications.status==='verified'만 필터링함"
  - "SRC-03: matching 모듈이 학습 목표·수준·기한·예산·희망시간을 저장하고, waitlist 등록/철회가 예약 관련 리포지토리를 전혀 참조하지 않는 구조로 되어 있음"
