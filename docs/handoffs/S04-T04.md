# Handoff: S04-T04

- task_id: S04-T04
- agent: A6(QA·품질 검증)
- status: PASS
- summary: |
    S04 단계(회원·수업·탐색·기본 추천)의 통합 QA 검증을 전면 수행하여 PASS를 판정했다.
    
    1. **백엔드 API 및 비즈니스 무결성 (45건 전수 PASS)**:
       `tests/integration/s04-discovery-and-matching.spec.ts`를 통해 역할별(교육자/학습자/운영자) 등록·가용시간·수업 등록,
       draft/pending_review/published/unpublished 상태머신 전이 및 비공개 수업 404 은닉, 타인 자원(수업 수정, 커리큘럼 버전 복제,
       학습 요청/대기 신청 조작) 수정 및 탈취 403 차단, 멱등성 키(Idempotency-Key) 동일 본문 캐시 및 다른 본문 409 충돌 차단을
       실제 NestJS 런타임에서 철저히 검증했다.
    
    2. **프론트엔드 이벤트 추적 및 추천 규칙 무결성 (11건 전수 PASS)**:
       `tests/integration/s04-frontend-and-events.spec.ts`를 통해 추천 응답(`recommendation_served`)과 뷰포트 진입(`recommendation_impression`)의
       분리 전송, 동일 세션 내 카드 노출 중복 제거(de-duplication), 사유 코드(`REASON_*`) 산정 및 점수 정렬, 조건 미완화 빈 결과 처리를 검증했다.
    
    3. **E2E 런타임 프로브 및 UI 11개 라우트 가용성 (20건 전수 PASS)**:
       `tests/e2e/s04-discovery-flow.spec.mjs`를 통해 런타임 API 4개 엔드포인트(헬스체크, 과목/생활권 참조 데이터, 공개 수업 목록)와
       S04-T03(A3)이 구현한 Next.js 11개 핵심 웹 라우트(`/`, `/login`, `/courses`, `/courses/demo`, `/recommendations`,
       `/learning-requests`, `/learning-requests/new`, `/tutor/courses`, `/tutor/courses/new`, `/tutor/profile`, `/tutor/availability`)의
       정상 응답(HTTP 200) 및 전체 13개 App Router 페이지 정적 빌드(Exit code 0)를 확인했다.
    
    4. **핵심 소스 규약(SRC-01~10) 100% 충족**:
       편입준비생 학교 미강제(SRC-01), 실명 비노출 및 공식 배지와 자기기재 분리(SRC-02), 대기 신청 결제/예약 미발생 및 대체 시간 제안(SRC-03),
       생활권과 대학 소속 분리(SRC-08), 신규 수업 규칙 기반 콜드스타트(SRC-10)의 준수성을 자동 테스트로 고정 검증했다.

- changed_files:
  - tests/integration/s04-discovery-and-matching.spec.ts (신규 — 45개 백엔드 통합 시나리오)
  - tests/integration/s04-frontend-and-events.spec.ts (신규 — 11개 프론트엔드 이벤트 및 추천 규칙 시나리오)
  - tests/integration/README.md (수정 — 테스트 위치 및 실행 방법 갱신)
  - tests/e2e/s04-discovery-flow.spec.mjs (신규 — E2E 20개 종합 프로브 스크립트)
  - tests/e2e/README.md (수정 — S04 E2E 20/20 PASS 상태 갱신)
  - docs/qa/S04-discovery.md (신규 — S04 상세 QA 종합 보고서)
  - docs/handoffs/S04-T04.md (수정 — PASS 완료 인수인계서)

- evidence:
  - base_revision: S04-T01 산출물(A2, PASS), S04-T03 산출물(A3, PASS), docs/handoffs/S04-T01.md, docs/handoffs/S04-T03.md
  - checks:
    - "pnpm exec vitest run tests/integration/s04-discovery-and-matching.spec.ts: 45건 전수 통과 (duration: ~1.86s, exit code 0)"
    - "pnpm exec vitest run tests/integration/s04-frontend-and-events.spec.ts: 11건 전수 통과 (duration: ~13ms, exit code 0)"
    - "pnpm exec vitest run tests/integration/: 통합 테스트 총 56건 전수 통과 (duration: ~6.59s, exit code 0)"
    - "node tests/e2e/s04-discovery-flow.spec.mjs: 런타임 API 4종 + 웹 라우트 11종 + SRC 규약 5종 총 20/20 PASS (exit code 0)"
    - "pnpm run --filter @campus-major-tutoring-mvp/web build: 성공 (exit code 0, 13개 페이지 정적 빌드)"
    - "pnpm -r run typecheck: 성공 (exit code 0, 타입 오류 0건)"
  - not_run:
    - "실제 결제 PG 및 실시간 WebSocket 채팅 — S05/S06 이후 단계 범위"
  - policy_and_metric_versions: "packages/contracts/openapi.yaml 0.2.0-s02-baseline, agent-prompts/source-alignment.md"

- remaining_work:
  - "S05 단계: 협의방(채팅) 및 실시간 제안/합의서 흐름 검증 (A6 S05-T05)"
  - "A0: S04-GATE 통합 판정 재실시"

- contract_requests: []

- next_owner: A0 (총괄·통합) — S04-GATE 완료 판정 진행

- source_requirement_evidence:
  - "SRC-01: 편입준비생의 학교 증명 없는 정상 학습 요청 등록을 자동 통합 테스트(시나리오 34) 및 프론트 폼 규약으로 100% 검증함."
  - "SRC-02: 공개 프로필에서 실명/연락처 비노출, verified 상태 증빙만 배지 노출, teachingEvidence(샘플설명, 예상결과물) 최상위 분리 반환을 자동 통합 테스트(시나리오 19, 20, 21)로 100% 검증함."
  - "SRC-03: 대기 신청 시 알림 동의(notifyConsent) 및 대체 시간 동의(alternativeTimeAccepted) 저장, 중복 신청 시 기존 레코드 재반환(isNew: false), 철회(status: withdrawn), 예약(bookings)/결제(payment_obligations) 미생성 구조적 격리를 자동 통합 테스트(시나리오 37, 38, 40, 42)로 100% 검증함."
  - "SRC-08: CandidateQueryService가 학교 소속(schoolAffiliation)을 필터 조건으로 쓰지 않고 생활권(lifeZoneId)만으로 매칭하여 타 학교/미재학 학습자도 동일 생활권이면 정상 후보로 반환함을 자동 통합 테스트(시나리오 24, 25)로 100% 검증함."
  - "SRC-10: 순수 TypeScript 서비스 기반으로 신규 수업(평가 0건)도 평점 0점으로 배제되지 않고 후보에 정상 포함되며, 시간 불일치 시 조건을 임의로 바꾸지 않고 timeMatches: false로 명시함을 자동 통합 테스트(시나리오 23, 26, 27)로 100% 검증함."
