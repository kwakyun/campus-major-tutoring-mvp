# 전공한시간 AI Agent 역할별 개발 분담

작성일: 2026-09-19 · 상태: 향후 작업 배분을 위한 계획 v1.0

기준 문서: [단계별 개발 로드맵](./전공한시간_단계별_소프트웨어_개발_로드맵.md), [웹서비스 아키텍처](./전공한시간_웹서비스_아키텍처.md), [AI·추천 아키텍처](./전공한시간_AI_추천_아키텍처.md)

역할별 실제 전달용 지시문은 [단계별 YAML 명령 패키지](./agent-prompts/README.md)에 10단계·65개 명령으로 정리되어 있다.

현재 프로젝트에는 기획·설계 문서가 있다. 아래 역할은 향후 AI 에이전트에게 일을 배분하기 위한 구분이며, 에이전트를 실행했거나 서비스 구현을 완료했다는 뜻이 아니다. 코드 경로는 프로젝트 기반 구축 때 생성할 예정 경로다.

## 1. 권장 구성: 총괄 1개 + 전문 역할 7개

**기능별 담당 영역을 유지하면서, 단계마다 작은 작업을 배분한다.** ‘백엔드 전체를 완성하라’는 한 번의 큰 지시보다 ‘제안 버전 저장’, ‘오래된 제안 수락 차단’처럼 결과와 검증 방법이 분명한 단위로 맡긴다.

| ID | 에이전트 역할 | 주 책임 | 최종 결과물 |
|---|---|---|---|
| A0 | 총괄·제품·통합 | 요구사항·우선순위·작업 배분·통합 판정 | 작업 목록, 결정 기록, 통합된 단계별 결과 |
| A1 | 아키텍처·DB·API 계약 | 공통 인터페이스·스키마·상태·권한 명세 | OpenAPI, migration, 데이터·이벤트 계약 |
| A2 | 핵심 업무 백엔드 | 회원·수업·협의·예약·수업 운영 | NestJS 핵심 업무 모듈 |
| A3 | UX·프론트엔드 | 학습자·교육자·운영자 화면과 API 연결 | Next.js 화면·공통 UI·사용 흐름 |
| A4 | 결제·보증금·정산 | 금액 계산·모의/실제 결제·환불·원장·지급 | Billing 모듈, PG Adapter, 거래 복구 |
| A5 | AI·추천·데이터 | 기본 추천·이벤트 분석·의미 검색·AI 작성 보조 | 추천 모듈, FastAPI, 모델 평가 |
| A6 | QA·품질 검증 | 독립 시나리오·권한·동시성·통합 검증 | 테스트·재현 절차·검증 보고서 |
| A7 | 개발 기반·배포·운영 | 프로젝트 초기화·CI·환경·배포·모니터링·복구 | 실행·배포 기반, 운영 절차 |

8개 역할은 8개 동시 실행을 뜻하지 않는다. 초기에는 **A0 + 현재 필요한 전문 역할 2~3개**를 운영하고, 앞선 산출물이 준비되면 다음 역할에 작업을 넘기는 방식을 권장한다. 같은 역할을 여러 작업에서 재사용할 수 있다.

## 2. 공통 분담 원칙

1. **작업 하나에 구현 책임자를 한 명 둔다.** 협력자가 많아도 해당 작업의 완료 결과를 제출하는 담당자는 하나다.
2. **명세를 맞춘 뒤 병렬 구현한다.** 필드명·오류 코드·권한·상태를 각 담당자가 따로 정하지 않는다.
3. **공통 파일은 정해진 담당자만 수정한다.** API 계약·DB migration·루트 설정·lockfile을 동시에 고치지 않는다.
4. **화면을 포함한 작은 기능 단위로 자주 통합한다.** 전체 웹·백엔드·AI를 각각 다 만든 뒤 처음 연결하지 않는다.
5. **구현자가 자기 테스트를 수행하고 QA가 통합 결과를 별도로 확인한다.** QA가 있다는 이유로 각 담당자가 검증을 생략하지 않는다.
6. **미정 사업 정책은 결정 대기 항목으로 남긴다.** 보증금·취소·가격 정책의 제안을 이미 확정된 것으로 구현하지 않는다. 모의 기능은 정책 설정을 주입해 계속 진행할 수 있다.
7. **추천 결과와 거래 상태의 최종 권한을 구분한다.** AI가 예약·결제·인증 상태를 바꾸지 않는다.
8. **검증한 것과 아직 검증하지 못한 것을 구분한다.** 실행하지 않은 테스트, 연결하지 않은 실결제, 확보하지 않은 사용자 성과를 완료로 보고하지 않는다.

## 3. A0 — 총괄·제품·통합 에이전트

**입력:** 사업계획서, 기존 아키텍처, 단계별 로드맵, 사용자 결정, 각 역할의 결과 보고.

**담당 과정**

- 1단계: 사용자 역할·P0 범위·확정 규칙·미정 정책을 요구사항으로 정리한다.
- 2~3단계: A1의 계약과 A7의 환경 준비를 확인하고 실제 작업 순서를 정한다.
- 4~7단계: 기능별 작업을 분할해 배분하고 통합 결과를 사용자 흐름으로 확인한다.
- 8단계: 제공된 파일럿 기록·결함·지표를 분석하여 개선 우선순위를 정한다.
- 9~10단계: AI·그룹·복수 차시 등 다음 기능의 진입 조건을 판단한다.

**직접 관리할 산출물:** `docs/product/`, `docs/decisions/`, `docs/tasks/index.md`, `docs/releases/`.

**경계:** 사용자 의사결정이 필요한 미정 사업 정책을 임의로 확정하지 않는다. 다른 역할의 코드를 동시에 고치지 않고 소유자에게 수정 작업을 배분한다. 통합 때문에 공통 파일 수정이 필요하면 해당 담당자의 작업과 순서를 맞춘다.

**완료 기준:** 작업마다 담당·선행 작업·수정 범위·완료 조건·현재 상태가 있고, 완료 판정이 실제 검증 결과와 연결된다. 중요한 실패가 남은 기능을 출시 준비 완료로 표시하지 않는다.

**초기 작업 예:** `PM-001 P0 요구사항 명세`, `PM-002 정책 결정표`, `PM-003 첫 개발 작업 목록`.

**배분용 역할 지시문**

```text
너는 전공한시간의 총괄·제품·통합 담당이다.
기준 문서에서 첫 파일럿의 범위와 확정 규칙을 추출하고, 미정 정책은 구분하라.
각 작업을 담당 역할, 선행 작업, 수정 범위, 검증 가능한 완료 기준으로 나눠라.
한 번에 전체 서비스를 맡기지 말고, 선행 조건이 충족된 작은 작업만 배분하라.
산출물을 통합한 결과를 확인하고, 미검증 기능과 남은 결정을 명시하라.
다른 역할과 같은 파일을 동시에 편집하지 말고 작업 상태표는 네가 관리하라.
```

## 4. A1 — 아키텍처·DB·API 계약 에이전트

**입력:** A0의 요구사항·정책 결정표, 기존 아키텍처, 각 구현 담당자의 계약 변경 요청.

**담당 과정**

- 2단계: 화면·서버가 공유할 API, 업무 상태, 권한, 금액·시간·이벤트 형식을 상세화한다.
- 3단계: 초기 스키마·고유 제약·DB 권한·migration과 데모 seed를 만든다.
- 4~7단계: 회원·수업·제안·동의·예약·결제 데이터의 변경을 관리한다.
- 9단계: 검색 사본·벡터·AI 작업·모델 버전 스키마를 추가한다.
- 10단계: 그룹 참여·회차별 금액·부분 환불 등의 모델 변경을 관리한다.

**직접 관리할 산출물:** `packages/contracts/`, `db/schema/`, `db/migrations/`, `db/seeds/`, `docs/architecture/`, `docs/api/`.

**경계:** 업무 API의 실행 코드와 화면은 각 구현 담당자가 작성한다. 이미 적용된 migration을 고쳐 과거 DB 상태를 바꾸지 않고 새 migration으로 변경한다. 생성된 API 클라이언트는 명세에서 다시 생성하며 다른 담당자가 직접 편집하지 않는다.

**완료 기준:** 빈 DB에 migration을 적용할 수 있고, API 요청·응답·오류·권한·상태·중복 요청 처리 규칙이 명확하다. 변경 계약은 기존 소비자 영향과 전환 방법을 포함한다.

**초기 작업 예:** `ARC-001 핵심 API 명세`, `DB-001 계정·수업 스키마`, `DB-002 제안·동의·예약 스키마`.

**배분용 역할 지시문**

```text
너는 전공한시간의 아키텍처·DB·API 계약 담당이다.
배정된 기능의 API와 데이터 모델을 구체화하고 상태·권한·오류를 정의하라.
계약과 migration은 담당 범위에서 관리하고, API 실행 코드와 화면은 구현 담당자에게 넘겨라.
예약의 시간 중복, 제안 버전, 동의 기록, 금액 정합성 제약을 명시하라.
필드나 이벤트 변경 시 웹·업무 서버·결제·AI에 미치는 영향을 보고하라.
```

## 5. A2 — 핵심 업무 백엔드 에이전트

**입력:** A1의 API·DB 계약, A7의 서버 실행 기반, A0의 작업 카드.

**담당 과정**

- 3단계: 인증 세션 검증·업무 권한·공통 서버 오류·요청 ID 기반을 연결한다.
- 4단계: 회원·교육자 심사·수업·가능 시간·학습 요청·수동 매칭 API를 구현한다.
- 5단계: 협의방·메시지·제안 버전·수락·예약 스냅샷·시간 점유를 구현한다.
- 6단계: 완료 확인·후기·차단·신고·분쟁 업무와 알림·outbox 기반을 구현한다.
- 7단계: 결제 의무 충족·분쟁 결정 이벤트를 예약 상태와 연결한다.
- 10단계: 그룹 참여·회차별 운영 등 선택된 업무 확장을 구현한다.

**직접 관리할 코드:** `apps/api/src/modules/identity/`, `catalog/`, `matching/`, `negotiation/`, `booking/`, `trust/`, `notifications/`, 공통 인증·업무 인프라, `packages/domain/time/`.

**경계:** 결제 승인·환불·원장·지급 코드는 A4가 담당한다. 추천 순위와 모델 호출은 A5가 담당한다. 모든 권한을 각 모듈에 복제하지 않고 검증된 공통 인증·권한 경로를 제공한다. DB 변경은 A1에 요청한다.

**완료 기준:** 인증된 사용자만 자기 자원에 접근하고, 오래된 제안·중복 수락·겹치는 시간 예약을 서버와 DB에서 거절한다. 거래 상태를 브라우저 요청대로 임의 수정하지 않는다.

**작업 예:** `BE-001 교육자 수업 등록`, `BE-002 가능 시간 조회`, `NEG-001 메시지 저장`, `NEG-003 이전 제안 수락 차단`, `BOOK-001 예약·시간 점유 생성`.

**배분용 역할 지시문**

```text
너는 전공한시간의 핵심 업무 백엔드 담당이다.
배정된 회원·수업·협의·예약·운영 기능을 합의된 API와 DB 계약에 맞춰 구현하라.
현재 사용자 권한과 업무 상태를 검사하고, 중요한 상태 변경은 트랜잭션으로 묶어라.
결제는 Billing의 공개 인터페이스를 호출하고 결제 테이블을 직접 수정하지 마라.
담당 모듈의 테스트와 실제 DB가 필요한 검증을 수행하고 실패 사례를 보고하라.
```

## 6. A3 — UX·프론트엔드 에이전트

**입력:** A0의 사용자 시나리오, A1의 API 계약, 실제 또는 명시적으로 모의인 API.

**담당 과정**

- 2단계: 학습자·교육자·운영자의 화면 흐름과 와이어프레임을 작성한다.
- 3단계: 레이아웃·폼·카드·오류·로딩·빈 상태 등 공통 UI를 만든다.
- 4단계: 로그인·프로필·수업 등록·가능 시간·탐색·기본 추천 화면을 구현한다.
- 5단계: 협의방·제안 카드·수락·최종 합의서·예약 상세를 연결한다.
- 6~7단계: 수업료·양측 보증금·예상 정산액·취소·후기·운영 화면을 연결한다.
- 9단계: 추천 이유·AI 초안 검토·작업 대기·실패 화면을 구현한다.
- 10단계: 선택된 그룹·복수 차시·개인화 UI를 구현한다.

**직접 관리할 코드:** `apps/web/`, `packages/ui/`, `docs/ux/`.

**경계:** 클라이언트 계산을 실제 청구·정산 금액의 근거로 사용하지 않는다. 권한은 화면 표시뿐 아니라 서버에서 검증되어야 한다. API가 준비되지 않았을 때는 명세에 맞춘 mock을 사용하고, 실제 연결 완료와 구분한다.

**완료 기준:** 모바일 화면에서 핵심 흐름을 수행할 수 있고, 합의·결제 대기·예약 확정·환불 대기 상태가 명확하다. 서버 오류를 사용자에게 설명하며, AI 생성 결과는 검토 후 확정하도록 연결한다.

**작업 예:** `WEB-001 수업 목록·상세`, `WEB-002 교육자 등록`, `WEB-003 제안 카드·최종 동의`, `WEB-004 예약·금액 명세`, `WEB-005 운영자 거래 화면`.

**배분용 역할 지시문**

```text
너는 전공한시간의 UX·프론트엔드 담당이다.
배정된 사용자 흐름을 Next.js 화면으로 구현하고 합의된 API 계약에 연결하라.
정상 화면뿐 아니라 로딩·빈 결과·권한 오류·오래된 제안·결제 확인 중 상태를 구현하라.
수업료·보증금·수수료·정산 예정액을 구분하고 서버가 확정한 값을 표시하라.
mock 연결과 실제 API 연결을 명확히 보고하고 모바일 핵심 흐름을 확인하라.
```

## 7. A4 — 결제·보증금·정산 에이전트

**입력:** 확정 수수료 규칙, 미정 정책의 상태, A1의 금액·납부·결제 계약, A2의 예약 인터페이스.

**담당 과정**

- 2~5단계: 수수료·납부 의무·보증금·원장·환불·지급 인터페이스를 A1·A2와 정리한다.
- 6단계: 모의 결제와 원장, 정상·실패·결과 불명·중복 처리 흐름을 구현한다.
- 7단계: 확인된 PG·보증금·지급 방식으로 승인·조회·환불·반환·정산을 연결한다.
- 8단계: 내부 거래와 제공자 기록을 대조하고 운영 실패의 복구 경로를 보완한다.
- 10단계: 그룹별 납부, 회차별 배분, 부분 환불·수수료 조정을 구현한다.

**직접 관리할 코드:** `apps/api/src/modules/billing/`, `packages/domain/money/`, `docs/payments/`와 해당 모듈 테스트.

**경계:** 예약 상태·일정 점유는 A2의 Booking 인터페이스로 변경한다. PG가 지원하는지 확인되지 않은 보증금 보관·보상 방식을 가능한 것으로 가정하지 않는다. 실제 수수료 15%와 검증용 보증금·손익 가정을 구분한다.

**완료 기준:** 승인·환불·지급 결과가 불명확하면 조회·대사로 복구하며, 재시도·중복 이벤트로 금액이 두 번 처리되지 않는다. 수업료·보증금·피해자 보상·수수료가 분리되고 관련 분쟁 중 지급이 보류된다.

**작업 예:** `PAY-001 금액 명세·납부 의무`, `PAY-002 Fake Adapter`, `PAY-003 승인·조회·웹훅`, `PAY-004 환불·보증금 처분`, `PAY-005 정산·대사`.

**배분용 역할 지시문**

```text
너는 전공한시간의 결제·보증금·정산 담당이다.
배정된 금액 처리 기능을 Billing 모듈과 Adapter로 구현하라.
교육자 수수료 15%, 학습자 플랫폼 수수료 0원 규칙을 지켜라.
모의·테스트·실제 결제를 구분하고, 결과 불명·중복 요청·중복 웹훅을 처리하라.
예약 원본은 직접 수정하지 말고 Booking 인터페이스와 합의된 트랜잭션 경계를 사용하라.
금액 정합성·재시도·환불·분쟁 중 지급 차단을 검증하고 외부 의존 사항을 보고하라.
```

## 8. A5 — AI·추천·데이터 에이전트

**입력:** 공개 수업·가능 시간 조회 계약, 추천·노출 이벤트 계약, 학습 목표 예시, AI 예산·데이터 사용 범위.

**담당 과정**

- 2단계: 추천 입력·결과·이유 코드·노출 이벤트·모델 버전 계약을 A1에 제안한다.
- 4단계: 조건·태그 기반 기본 추천을 NestJS에 구현한다. Python 없이도 작동하게 한다.
- 6단계: 노출→상담→합의→납부→완료를 연결해 이벤트 품질과 지표를 확인한다.
- 8단계: 공급 부족·시간 문제·설명 문제와 순위 문제를 구분해 분석한다.
- 9단계: FastAPI, 임베딩 worker, pgvector 검색, 작성 보조, 비용·지연·버전 관리와 평가를 구현한다.
- 10단계: 데이터가 충분할 때 개인화·학습 순위 모델을 평가·도입한다.

**직접 관리할 코드:** `apps/api/src/modules/recommendations/`, `services/ai/`, `docs/ai/`와 해당 영역의 분석·평가 코드.

**경계:** 공개·거래 가능 상태, 시간 충돌, 차단 관계는 A2가 제공한 업무 조회를 사용하고 NestJS에서 최종 검사한다. Python은 후보 점수·벡터·초안을 반환하며 예약·결제·인증을 수정하지 않는다. 거래 이벤트는 A2·A4가 상태 변경과 함께 발행하고, A5가 임의로 실제 완료 이벤트를 생성하지 않는다.

**완료 기준:** 신규 사용자·신규 교육자도 기본 추천을 받을 수 있다. AI 실패 시 기본 추천으로 전환되고 부적격·삭제 수업은 제외된다. 모델 변경·원문 수정·삭제가 파생 데이터에 반영되며 평가 결과와 비용을 재현할 수 있다.

**작업 예:** `REC-001 조건·태그 추천`, `DATA-001 노출·전환 분석`, `AI-001 한국어 평가셋`, `AI-002 임베딩 갱신`, `AI-003 의미 검색`, `AI-004 초안 생성`.

**배분용 역할 지시문**

```text
너는 전공한시간의 AI·추천·데이터 담당이다.
먼저 기본 추천과 평가 기반을 만들고 배정 단계에 따라 의미 검색·작성 보조를 추가하라.
필수 조건을 만족하는 후보 안에서 순위를 계산하고 추천 이유는 확인 가능한 값으로 구성하라.
AI 실패·비용 한도 초과 시 기본 추천을 유지하고 거래 기능에 영향을 주지 마라.
신원 자료·계좌·전체 채팅을 기본 입력으로 사용하지 마라.
모델·데이터 버전과 평가 결과, 미검증 사항을 보고하라.
```

## 9. A6 — QA·품질 검증 에이전트

**입력:** 요구사항과 완료 기준, API·상태·권한 계약, 각 구현 결과, 테스트 환경.

**담당 과정**

- 1~2단계: 요구사항에서 정상·실패·권한·동시성 시나리오를 도출한다.
- 3단계: 실제 DB와 브라우저 테스트 실행 환경을 A7과 연결한다.
- 4~6단계: 역할별 접근·수업 탐색·재제안·예약·모의 거래·운영 흐름을 검증한다.
- 7단계: 금액 조작·중복 처리·응답 유실·분쟁 중 지급·복구를 검증한다.
- 8단계: 사용자 결함을 재현하고 중요 변경의 회귀 여부를 확인한다.
- 9~10단계: AI 장애·삭제 데이터·후보 자격·그룹·부분 환불 등 추가 위험을 검증한다.

**직접 관리할 코드·문서:** `tests/integration/`, `tests/e2e/`, `tests/fixtures/`, `docs/qa/`.

**경계:** 도메인 구현자는 담당 모듈 내부의 규칙 테스트를 작성한다. QA는 독립된 통합·사용자 시나리오를 작성한다. 제품 코드 결함은 소유자에게 재현 방법과 함께 돌려보내며 테스트 기대값을 잘못된 구현에 맞춰 낮추지 않는다.

**완료 기준:** 테스트 대상 변경 버전·실행 명령·통과/실패·미실행 사유가 보고된다. 실행 환경 때문에 검증하지 못한 경우 통과로 판정하지 않는다. 실패 시 예상·실제 결과와 재현 입력을 남긴다.

**작업 예:** `QA-001 수락·시간 충돌 검증`, `QA-002 권한 경계 검증`, `QA-003 결제 중복·복구 검증`, `QA-004 전체 사용자 흐름`, `QA-005 AI 대체 경로`.

**배분용 역할 지시문**

```text
너는 전공한시간의 독립 QA 담당이다.
배정된 기능의 명세와 완료 조건을 기준으로 정상·실패·권한·동시성 사례를 검증하라.
구현과 같은 로직을 복사하는 테스트보다 잘못된 결과를 탐지하는 사례를 작성하라.
실패하면 재현 입력, 예상·실제 결과, 영향 범위, 담당 역할을 보고하라.
통과한 검증과 미실행 검증을 구분하고 검증 대상 버전을 명시하라.
```

## 10. A7 — 개발 기반·배포·운영 에이전트

**입력:** 아키텍처의 배포 구조, 각 앱 실행 조건, DB migration 절차, 작업별 환경 요구.

**담당 과정**

- 3단계: 저장소 초기 구조, workspace 설정, 웹·API 실행 환경, CI, 스테이징을 준비한다.
- 4~6단계: 테스트 DB·예시 데이터 초기화 절차, 작업 실행 환경, 로그·상태 확인을 제공한다.
- 7단계: 비밀키·운영 환경 분리, migration 실행, 배포·복구·백업 복원·알림을 검증한다.
- 8단계: 장애·작업 적체·DB 연결·호출량·운영 비용을 관찰한다.
- 9단계: FastAPI·Python worker의 내부 연결과 독립 배포·리소스 제한을 구성한다.
- 10단계: 실제 병목이 확인된 실행 단위만 분리·증설한다.

**직접 관리할 파일:** `.github/workflows/`, `infra/`, `scripts/`, 루트 workspace·lockfile·공통 빌드 설정, 배포용 Docker 파일, `docs/operations/`.

**경계:** 초기 골격 생성 중에는 A7이 앱 시작 파일을 작성할 수 있다. 골격 인수인계 후에는 웹은 A3, 업무 서버 시작·연결 코드는 A2, AI 앱은 A5로 소유권을 넘긴다. migration 내용은 A1이 작성하고 A7은 적용·복구 절차를 관리한다. 결제 재처리의 업무 판단은 A4·A2의 API를 통한다.

**완료 기준:** 문서에 따라 개발·스테이징을 재현하고, 운영과 테스트의 DB·키·예시 데이터를 구분한다. 중단된 작업을 복구할 수 있고 복원 시험의 결과가 남는다. 환경 파일·로그에 비밀값을 노출하지 않는다.

**작업 예:** `OPS-001 workspace 초기화`, `OPS-002 CI·스테이징`, `OPS-003 작업 실행·모니터링`, `OPS-004 복원·배포 복구`, `OPS-005 AI 실행 환경`.

**배분용 역할 지시문**

```text
너는 전공한시간의 개발 기반·배포·운영 담당이다.
배정된 실행·CI·배포·복구 환경을 구성하고 다른 역할이 재현할 수 있게 문서화하라.
골격 생성 후 앱 구현 파일의 소유권을 담당자에게 넘겨 중복 수정을 피하라.
DB migration은 A1의 결과를 적용하고 업무·금액 규칙을 인프라 코드에 구현하지 마라.
환경 분리, 비밀값 보호, 작업 중단 복구, 백업 복원 결과를 확인해 보고하라.
```

## 11. 단계별 실제 배분표

| 기존 로드맵 단계 | 핵심 담당 | 병행할 담당 | 통합해서 확인할 결과 |
|---|---|---|---|
| 1 요구사항 | A0 | A1: 모델 위험, A6: 검증 조건 | P0·정책·완료 기준 |
| 2 상세 설계 | A1 | A3: 화면, A2·A4·A5: 계약 제안, A6: 시나리오 | 같은 필드·상태·권한을 사용하는 설계 |
| 3 기반 구축 | A7 | 골격 전달 후 A1: DB, A2: 인증, A3: UI, A6: 테스트 | 실행·인증·DB·빌드 |
| 4 회원·수업·탐색 | A2·A3의 개별 작업 | A5: 기본 추천, A1: 변경 DB, A6: 권한 검증 | 등록→탐색→상담 진입 |
| 5 협의·예약 | A2·A3의 개별 작업 | A4: 금액 인터페이스·모의 결제 준비, A6: 동시성 | 제안→동의→중복 없는 예약 |
| 6 전체 이용 흐름 | A2·A3·A4의 개별 작업 | A5: 전환 분석, A6: 통합, A7: 작업 환경 | 완료·취소·분쟁을 포함한 모의 MVP |
| 7 실결제·출시 준비 | A4·A7의 개별 작업 | A2: 거래 상태 연결, A3: 실패 화면, A6: 독립 검증 | 실제 거래와 복구 준비 |
| 8 파일럿 | A0 | A6·A7: 결함·운영, A5: 분석, 소유자별 개선 | 거래 대사·사용성·전환 근거 |
| 9 AI | A5 | A1: 파생 데이터, A3: AI UI, A6: 검증, A7: 실행 | 평가한 AI 검색·초안·대체 경로 |
| 10 확장 | A0가 확장 작업별 담당 결정 | 개인화 A5 / 그룹 A2·A3·A4 / 실행 확장 A7 | 선택한 기능의 검증 결과 |

여러 ID가 있는 칸은 하나의 작업을 공동 소유하라는 뜻이 아니다. 예를 들어 ‘제안 수락 API’는 A2, ‘제안 수락 화면’은 A3처럼 작업을 분리하며 A0가 하나의 사용자 흐름으로 통합한다.

## 12. 처음 실행할 작업 순서

### 묶음 1 — 범위와 공통 계약

- A0: `PM-001` 요구사항과 정책 결정표.
- A1: 요구사항을 받아 핵심 API·DB·상태 계약.
- A3: 시나리오 기준 화면 흐름. 필드·오류 표시를 A1과 맞춤.
- A6: 명세에서 예약·권한·금액 검증 사례 작성.

**인수인계:** 첫 범위와 계약 버전이 정리되면 기반 구축으로 진행한다. UI와 API 설계는 제안안을 병행할 수 있지만 서로 다른 계약으로 구현을 시작하지 않는다.

### 묶음 2 — 개발 환경과 첫 수업 탐색

- A7: 프로젝트 골격과 실행·빌드 명령을 먼저 전달.
- A1: 회원·수업·가능 시간 migration.
- A2: 인증·수업·가능 시간 API.
- A3: 계약 기반 mock으로 화면 구현 후 실제 API 연결.
- A5: 후보 조회 계약을 사용한 기본 추천. 미완성 후보 API는 테스트 대역으로 검증.
- A6: 통합된 등록·탐색·권한 확인.

**인수인계:** 실제 계정의 교육자가 등록한 수업을 다른 계정이 찾아볼 수 있어야 한다.

### 묶음 3 — 협의·예약과 모의 거래

- A1: 제안·동의·예약·납부·원장 계약과 migration.
- A2: 협의·예약·시간 점유.
- A3: 제안 카드·최종 동의·예약 명세.
- A4: 합의된 Billing 계약으로 모의 결제·환불·정산 구현.
- A5: 수업 이력이 없어도 가능한 AI 평가 질의 준비, 실제 거래와 분리된 실험.
- A6: 최신안 수락·중복 예약·미납·분쟁·전체 모의 흐름 검증.

**인수인계:** 사용자가 합의하고 예약을 만들며, 운영자는 모의 완료·취소·분쟁을 처리할 수 있어야 한다.

### 묶음 4 — 실거래 준비와 제한 운영

- A4: 확인된 결제·보증금·지급 방식 연결.
- A2·A3: 결제 결과·정산 보류·실패 상태 연결.
- A7: 운영 환경·대사 작업·복구·알림.
- A6: 중복·응답 유실·권한·실패 복구 검증.
- A0: 준비 상태와 남은 외부 의존 사항을 정리하고 파일럿 개선 작업 배분.

**인수인계:** 모의 검증만 끝난 상태를 유료 운영 준비 완료로 간주하지 않는다. 계약이나 정책이 미정이면 해당 실거래 경로만 대기시키고 독립적인 개발은 계속한다.

### 묶음 5 — AI와 선택적 확장

- A5: 의미 검색·임베딩 갱신·초안·평가·기본 추천 전환.
- A1·A3·A7: 파생 데이터·사용자 화면·AI 실행 환경 연결.
- A6: AI 실패·비공개 수업·버전·데이터 삭제 검증.
- A0: 실제 사용 근거에 따라 개인화·그룹·복수 차시 중 다음 작업 선택.

AI 실험은 앞선 묶음과 병행할 수 있다. 실제 노출·전환 데이터가 필요한 개인화 학습은 그 데이터가 준비된 뒤 진행한다.

## 13. 충돌하기 쉬운 공유 영역의 소유권

| 영역 | 수정 담당 | 다른 역할의 요청 방식 |
|---|---|---|
| 요구사항·작업 상태표 | A0 | 결정 제안·상태 보고 |
| OpenAPI·이벤트·생성 클라이언트 | A1 | 변경 이유·입출력·영향 제출 |
| DB schema·migration·공통 demo seed | A1 | 필요한 테이블·제약·샘플 데이터 제출 |
| 인증 guard·공통 API 부트스트랩·outbox 실행 기반 | A2 | 필요한 권한·핸들러 계약 요청 |
| 공통 UI·운영자 화면 | A3 | 표시 데이터·상태·권한 요구 제출 |
| 금액 계산 공통 코드 | A4 | 정책·예시·기대 금액 전달 |
| 추천 모듈·AI 서비스 | A5 | 후보 조회·최종 자격 검사 계약 사용 |
| 통합·E2E 테스트와 전용 fixture | A6 | 테스트 대상·실행 방법·데이터 조건 전달 |
| 루트 workspace·lockfile·CI·컨테이너 설정 | A7 | 의존성·실행 명령·환경 요구 제출 |

앱별 의존성 요구는 구현 담당자가 정하되 공유 lockfile 갱신은 A7이 순서대로 통합한다. 공통 코드를 추가할 위치가 불분명하면 A0가 소유자를 정한 뒤 작업 카드에 기록한다. 작업별 보고서는 `docs/handoffs/{TASK_ID}.md`로 나눠 각 작업 담당자가 작성하고, 요약 상태표는 A0만 갱신한다.

Git 기반 구현을 시작하면 작업별 브랜치 또는 격리된 작업 디렉터리를 사용한다. 공유 디렉터리에서 작업하는 경우 파일 소유권을 더 엄격히 지킨다. 각 결과는 기준 버전·변경 파일·검증 결과를 포함하고 A0가 통합 순서를 관리한다. 통합 후에는 변경된 연결 부분의 검증을 다시 실행한다.

## 14. 역할 간 필수 인수인계 계약

### A1 → A2·A3·A4·A5: 공통 명세

API 필드·권한·오류·상태·금액 단위·시간대·이벤트 형식을 전달한다. 변경이 필요하면 소비자 영향과 함께 새 계약을 만든다. 구현자가 오류를 피하려고 각자 다른 임시 필드를 추가하지 않는다.

### A2 ↔ A4: 예약과 돈

A2는 예약·합의·납부 의무 생성 요청을 담당하고 A4는 Billing의 납부·결제·원장·반환·지급을 담당한다. 같은 DB에서 원자성이 필요한 내부 변경은 A1이 정한 트랜잭션 계약을 사용한다. 외부 PG 호출은 DB 잠금 밖에서 실행한다.

결제 의무가 모두 충족되었다는 Billing 결과를 Booking이 다시 확인해 예약을 확정한다. 분쟁 접수는 관련 지급 보류와 일관되게 연결한다. 금융 작업 완료 전에 UI나 예약 상태만 먼저 성공으로 변경하지 않는다.

### A2 ↔ A5: 수업 후보와 추천

A2가 최신 공개 상태·가능 시간·차단·예약 충돌을 검사할 업무 조회 계약을 제공한다. A5의 NestJS 추천 모듈은 이를 사용해 후보를 구하고 결과를 재검사한다. Python 모델 점수는 참고 결과이며 자격 검사나 예약 검증을 대체하지 않는다.

### A2·A4·A3 → A5: 실제 이용 이벤트

거래 상태의 진실은 A2·A4의 서버 이벤트다. A3는 실제 노출·클릭을 전송하고 A5가 지표로 연결한다. 클라이언트가 보낸 ‘결제 완료’를 실제 거래 완료로 집계하지 않는다.

### 구현 담당 → A6 → A0: 검증과 통합

구현 담당이 변경 내용·자체 검증·재현 데이터를 제출하면 A6가 해당 버전에서 독립 검증한다. A0는 통합 버전의 필수 검증이 충족됐는지 확인해 작업을 완료 처리한다. 실패하면 코드 소유자에게 수정 작업을 돌려보낸다.

## 15. 실제 작업 배분에 사용할 공통 양식

역할 지시문만 보내면 범위가 너무 넓다. 매번 다음 작업 카드와 함께 전달한다.

```text
작업 ID:
담당 역할:
목표: 사용자가 할 수 있어야 하는 행동 한 가지
기준 문서·버전:
선행 작업과 현재 준비 상태:
입력 API·데이터·샘플:
수정 허용 경로:
다른 담당자가 관리하는 경로:
이번 작업에 포함할 것:
다음 작업으로 남길 것:
완료 기준:
검증 명령 또는 수동 확인 방법:
인수인계 대상:

반환 형식:
1. 구현·작성 결과
2. 변경 파일과 결과 위치
3. 실행한 검증·결과·대상 버전
4. 미실행 검증·미완료 사항·외부 의존
5. 다른 역할에 필요한 계약 변경 또는 후속 작업
```

### 배분 예시: A2에게 맡길 작은 작업

```text
작업 ID: NEG-003
담당 역할: A2 핵심 업무 백엔드
목표: 재제안 후 이전 제안을 수락하여 예약하는 일을 차단한다.
선행: 제안 저장·협의방 권한·현재 제안 버전 조회 계약이 준비됨.
수정 범위: negotiation 모듈과 해당 모듈의 테스트.
경계: DB migration은 A1, 화면은 A3, Billing은 A4가 담당한다.
완료 기준:
  - v2 생성 후 v1 수락은 STALE_PROPOSAL로 거절한다.
  - 중복 수락은 중복 예약을 만들지 않는다.
  - 타 협의방 참여자의 수락 요청을 거절한다.
  - 재제안과 수락의 동시 요청 결과가 계약과 일치한다.
검증: 규칙 테스트 및 실제 PostgreSQL을 이용한 동시성 검증.
인수인계: A3에는 오류 응답 예시, A6에는 재현 입력과 실행 결과를 전달한다.
```

## 16. 작업 상태와 완료 판정

작업 상태는 `대기 → 실행 가능 → 진행 중 → 검증 중 → 통합 완료`로 관리한다. 미정 정책·외부 환경·선행 작업에 막힌 경우 이유를 기록하고 해당 작업만 대기시킨다. 다른 독립 작업을 함께 멈출 필요는 없다.

| 상태 | 의미 |
|---|---|
| 대기 | 아직 선행 계약·결정·환경이 준비되지 않음 |
| 실행 가능 | 입력·수정 범위·완료 기준이 준비됨 |
| 진행 중 | 담당자가 구현·작성 중 |
| 검증 중 | 담당자 결과를 제출했으며 통합·독립 검증 중 |
| 통합 완료 | 통합된 버전에서 필요한 검증을 통과함 |

파일이 생성되었다는 사실만으로 기능을 완료 처리하지 않는다. 실제 API 대신 mock을 연결한 화면, 제공자 미연결 결제, 평가하지 않은 추천은 그 상태를 결과에 명시한다.

**첫 배분은 A0의 요구사항·작업 목록과 A1의 공통 계약부터 시작한다.** 그다음 A7의 실행 골격을 준비하고, A2·A3·A5가 수업 등록·탐색 기능을 나눠 구현하며 A6가 통합 결과를 확인하는 순서가 적합하다.
